# Hoist node_modules so native bindings (e.g. duckdb's .node binary) land at
# standard paths that Next.js's file tracer can follow without chasing pnpm
# symlinks. Required for Vercel deploys.
node-linker=hoisted
